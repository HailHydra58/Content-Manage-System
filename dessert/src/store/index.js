import { createStore } from "vuex";

export default createStore({
  state: {
    isLogin: false,
    loginUser: "user",
    loginRoot: null,
    loginStuId: null,
    showLeftNum: 1,
  },
  mutations: {},
  actions: {},
  getters: {},
  modules: {},
});
