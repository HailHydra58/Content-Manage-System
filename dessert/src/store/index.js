import { createStore } from "vuex";

export default createStore({
  state: {
    isLogin: false,
    loginUser: "user",
    showLeftNum: 1,
  },
  mutations: {},
  actions: {},
  getters: {},
  modules: {},
});
